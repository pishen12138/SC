#include <stdio.h>
int main()
{
	int max(int x, int y);
	int a, b, c;
	int A, B, C;
	printf("这是一个比较三个整数大小的程序\n");
	printf("将输入法切换为英文，并输入三个整数，中间用，隔开\n");
	scanf_s("%d,%d,%d", &a, &b, &c);
	A = max(a, b);
	B = max(b, c);
	if (A == B)
		printf("最大的数是%d\n", A);
	else C = max(A, B);
		printf("最大的数是%d\n",C);
	return 0;
}
int max(int x, int y)
{
	int z;
	if (x > y)z = x;
	else z = y;
	return (z);
}
